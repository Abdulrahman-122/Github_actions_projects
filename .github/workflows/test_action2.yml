name: Test_Js_Action
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - id: Greet
        
        uses: ./actions/greet_action
        with:
          name: Abdulrahman
      - run: echo "${{steps.greet.outputs.message}}